<h2>My Companies</h2>
<div class="company_list">
  <ul>
    <% @companies.each do |co| %>
        <li>
          <h4><%= link_to co.name, :controller => 'company', :action => 'edit', :company_id => co.id %></h4>
        </li>

    <% end %>
  </ul>
</div>